ARG BASE=nvidia/cuda:12.4.1-cudnn-runtime-ubuntu22.04
FROM ${BASE} AS runtime

ENV PYTHONUNBUFFERED=1 \
    PYTHONHASHSEED=0 \
    MPLBACKEND=Agg \
    JAX_PLATFORMS=cpu \
    XLA_PYTHON_CLIENT_PREALLOCATE=false \
    SIMWORLD_ARTIFACT_ROOT=/work/artifacts \
    UV_LINK_MODE=copy

RUN apt-get update && apt-get install -y --no-install-recommends \
      build-essential git curl graphviz python3 \
    && rm -rf /var/lib/apt/lists/*

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

RUN useradd -m -u 1000 rw
WORKDIR /work
COPY pyproject.toml uv.lock README.md ./
COPY src ./src
# All extras: the pipeline needs bayes/causal/rl/opt/app and `make smoke` runs
# the slow (dev) tests; a --no-dev core-only sync cannot run the image's workload.
RUN uv sync --frozen --all-extras
# CUDA torch replaces the CPU wheel; keep in sync with torch floor in pyproject.toml
RUN uv pip install --index-url https://download.pytorch.org/whl/cu124 "torch>=2.4" --reinstall-package torch

COPY configs ./configs
COPY scripts ./scripts
COPY tests ./tests
COPY Makefile ./
RUN chown -R rw:rw /work
USER rw

ENTRYPOINT ["uv", "run"]
CMD ["python", "scripts/run_pipeline.py", "profile=smoke", "device=cuda"]
