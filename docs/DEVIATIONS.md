# Deviations from PLAN.md
Where the installed reality differed from this plan. Follow the library, not the plan.

| Date | Plan said | Reality | What I did | Why |
|---|---|---|---|---|
| 2026-07-19 | Docker build gates (Stage 16, §18) | No Docker daemon on this machine (macOS, not installed) | Dockerfiles + compose + CI workflows written; local `docker build` gates recorded as SKIPPED (env-limited); CI runs them on push | Cannot install Docker Desktop without the user; CI covers it |
| 2026-07-19 | §6 defaults list order: `profile` first, `dgp` before `network` | Hydra merges defaults in list order — later entries override earlier ones, so `profile` first would be overridden by later groups | Moved `dgp` after `network` and `profile` last in `configs/config.yaml` defaults | Profile is the scaling story and must win on size knobs; dgp variant must be able to set `network.homophily` |
| 2026-07-19 | `paths` use `${hydra:runtime.cwd}` | The `hydra:` resolver only resolves inside a Hydra app, which breaks config validation in plain pytest | Used relative paths + `${oc.env:REGWORLD_ARTIFACT_ROOT,artifacts}`; `hydra.job.chdir=false` keeps cwd at repo root | Same behavior, testable everywhere |
| 2026-07-19 | `causal` extra as listed in Appendix A | uv backtracked econml→sparse→numba to numba 0.53/llvmlite 0.36, unbuildable on py3.12 | Added `numba>=0.60` floor to the causal extra; everything then resolves | Guardrail 12: relax/steer the offending pin, don't drop EconML |
| 2026-07-19 | `dowhy>=0.11` | dowhy 0.11 imports `numpy.distutils`, removed in numpy 2.x | Floor raised to `dowhy>=0.12` (installs 0.12, imports clean) | Follow the library |
| 2026-07-19 | `arviz>=0.18` (open-ended) | arviz 1.2 removed `concat` and other APIs pymc 5.26 needs | Capped `arviz>=0.18,<1` (installs 0.23.4) | Two libraries must agree; 0.x is the API the plan's diagnostics use |

## Installed stack (Stage 0 probe, 2026-07-19, macOS arm64, Python 3.12)
numpy 2.4.6 · scipy 1.18.0 · pandas 2.3.3 · polars 1.42.1 · pyarrow 24.0 · duckdb 1.5.4 ·
networkx 3.6.1 · mesa 3.5.1 (AgentSet API confirmed; no RandomActivation) · torch 2.13.0 ·
torch-geometric 2.8.0 (HeteroConv imports, no torch_scatter) · gymnasium 1.2.2 (5-tuple confirmed) ·
xarray 2026.7.0 · zarr 2.18.7 · sklearn 1.6.1 · statsmodels 0.14.6 · hydra 1.3.4 · pydantic 2.13.4 ·
mlflow 3.14.0 · numpyro 0.21.0 · jax 0.11.0 · pymc 5.26.1 · arviz 0.23.4 · dowhy 0.12 · econml 0.16.0 ·
linearmodels 7.0 · causal-learn (imports) · stable-baselines3 2.9.0 · pettingzoo 1.26.1 · ray 2.56.1
(RLlib present) · torchrl 0.13.3 · tensordict 0.13.0 · SALib (imports) · optuna 4.9.0 · botorch 0.18.1 ·
ax 1.3.1 · streamlit 1.59.2 · agent-torch (imports; usability probed at Stage 3b) · matplotlib 3.11.1 ·
plotly 6.9.0. `.stage_skips` is empty — every extra resolved.
| 2026-07-19 | MLflow file backend (`file:./experiments/mlruns`) | MLflow 3.14 raises: filesystem store is in maintenance mode | Default tracking URI is `sqlite:///experiments/mlflow.db` | Still credential-free and offline; guardrail 8 wanted sqlite under concurrency anyway |
| 2026-07-19 | Gate 0 no-op override `'stages={}'` | Hydra merges `{}` into the all-true defaults — a no-op | No-op runs use `'~stages'` (delete node → StagesCfg all-False defaults) | Same intent, real Hydra semantics |
| 2026-07-19 | §7.4 q_it has no own-audit term, but the §7.7 DAG draws `audited → perceived_risk → compliant_next` | The two sections are inconsistent as written | Added a known constant `(1 + 0.8·audited_{t-1})` factor to q_it; Stage 1 reconstructs the same documented factor from observed audit history | Makes per-firm do(audited) well defined, which Stage 5's CATE grading needs |
| 2026-07-19 | §10 5f calls the staggered DiD “raising enforcement from e_low to e_high” | Regime-P onset simultaneously activates enforcement, phase salience, and compliance cost; it is not an enforcement-only intervention | Set the numeric anchors to 0.0/0.6 and label the answer-key estimand `total_regulation_onset_att`; Stage 5 must compare the same onset intervention or report the audit-only effect separately | The causal table must not claim a narrower intervention than the DGP ran |
| 2026-07-19 | `dgp=wellspecified` disables homophily and corr(z,size), but retains the omitted `beta_capacity*z` term | Even an independent omitted term changes a logistic link (non-collapsibility), so the fitted micro-likelihood cannot recover the planted slopes exactly | Omit the latent-capacity term only in the recovery-control world; keep it in `confounded` and `misspecified` | Makes “well specified” mathematically true while preserving the planted failure case |
| 2026-07-19 | About eight market edges per consumer segment | At smoke/dev/full sizes this leaves most firms with structurally zero demand, mechanically causing exits and concentration before policy acts | Give every firm one sector-matched market edge, then add the configured eight preferential links per segment | The backfire finding must arise from policy costs, not an uncovered graph |
| 2026-07-19 | Two degraded versions of every graph | Association membership is directly observed in the registry, and the current observation model does not define noise for market links | Keep membership and market edges shared/exact for now; degrade supply and influence, and report this limitation | Avoid inventing an unplanned error model; market/membership degradation remains a documented extension |
| 2026-07-19 | Relationship between Regime P's terminal state and Regime F's initial state is underspecified | Carrying P's quarter/compliance state into the distinct future CDPA makes phase-in start at q24 and begins near full compliance | Start Regime F as a fresh episode on the same generated entities/true graph, with a common q0 baseline and common random numbers across policies | P is an analogous past regulation, not the same compliance obligation |
| 2026-07-20 | Prefer AgentTorch for Stage 3b when it installs | AgentTorch 0.6 imports, but its package root exposes no simulation API and its usable path requires a registry, YAML model graph, controller, and substep scaffolding around the same tensor kernels | Use the PLAN-approved pure-PyTorch backend with sparse COO graph operations and straight-through Bernoulli transitions | Preserves the differentiable/scalable contract and makes Mesa-agreement directly testable without framework-specific configuration |
| 2026-07-20 | Mesa model's controlled `step(policy, actions)` can also serve Mesa's scheduler | Mesa 3.5 wraps `Model.step` as a zero-argument clock-advancing method | Keep `step()` zero-argument and add `step_with_controls(...)`, which queues controls and advances through Mesa's wrapper | Preserves Mesa's `steps`/`time` semantics while giving Gymnasium and PettingZoo an explicit controlled transition API |
| 2026-07-20 | Random rollout times supply not-yet-treated controls for the observed staggered DiD | IID draws on a short onset window can place every smoke cohort early (the original seed put all regions on by q6), leaving no credible late-period controls | Randomly assign region labels to a balanced q3–q15 onset grid, with the last cohort after the q12 observation window | Assignment remains independent of firm traits, while every observed quarter retains the not-yet-treated comparison the planned estimator requires |
| 2026-07-20 | Stage 4 describes “sixteen parameters” | Its tables specify 11 fitted micro quantities (nine coefficients plus q0/q1) and six macro quantities; latent beta_capacity is truth-only | Retain and report all 17 fitted quantities, with the stated recovery threshold of 12 kept as a compatibility floor | Silently dropping a named fitted parameter would make recovery look better by changing the denominator |
| 2026-07-20 | SMC-ABC directly evaluates the tensorized simulator for thousands of particles | Direct evaluation exceeded the smoke budget once design replicates and rounds were included | Fit an explicitly diagnosed ExtraTrees interpolator over real Latin-hypercube tensorized-ABM runs, then use it inside SMC; persist design and holdout MAE | This is the PLAN's named surrogate fallback, while preserving an auditable link to the full transition model |
| 2026-07-20 | ArviZ consumes NumPyro NUTS diagnostics transparently | `az.plot_energy` needs a `sample_stats.energy` field NumPyro only exposes when `mcmc.run(extra_fields=(...,"energy",...))` is set, and arviz 0.23's `summary(kind="all")` yields `hdi_5%`/`hdi_95%` columns (not the newer preview API) | Collect `energy`/`potential_energy`/`num_steps`/`accept_prob` as extra fields and read the 0.23 summary column names; added `test_micro_diagnostics_runs_full_arviz_and_energy_path` to cover the whole diagnostics path | The full `make calibrate` gate failed twice on these two integration points before the fields/columns were pinned to the installed versions |
| 2026-07-20 | `from econml.dml import LinearDML` just works | econml 0.16 eagerly imports `shap` in `_shap.py`; installed `shap` 0.46 crashes at import under numpy 2.4 (`np.dtype(np.floating)` removed), and the fix (shap ≥0.52) drags in pandas 3.0 / sklearn 1.9, which statsmodels/linearmodels do not yet support | Install a `sys.modules["shap"]` stub (`_ensure_econml_importable()` in `causal/estimate.py`) before importing econml; shap is used only inside `.shap_values()`, which the pipeline never calls | Keeps EconML (guardrail 12: don't drop it) and pandas 2.x with no lockfile change; SHAP explanations are not part of the plan |
| 2026-07-20 | dowhy refuters run out of the box | dowhy 0.12 calls `nx.algorithms.d_separated`, removed in networkx 3.5+; dowhy 0.14 fixes the call but force-downgrades scipy 1.18 → 1.15 | Alias `nx.algorithms.d_separated = nx.is_d_separator` before importing dowhy (`_ensure_dowhy_networkx_compat()` in `causal/refute.py`); identical signature | scipy 1.18 is required by the rest of the installed stack; the alias is exact |
| 2026-07-20 | §7.8/§10 assume the staggered DiD estimand equals the do() onset ATT ("we know the true effect the DiD should recover") | The DGP §7 has cross-region interference by construction: peer contagion and economy-wide macro trust spill into not-yet-treated regions (never-treated control cohort's true compliance rises 0.12 → 0.50 over q1-11), so the DiD's clean-comparison estimand (tau_did_truth = 0.358 at smoke seed) genuinely differs from the nobody-ever-treated ATT (tau_true = 0.415) | Sealed `tau_did_truth` (the same group-time estimator applied to the true panel of the as-scheduled rollout, averaged over do-seeds) and `tau_interference_gap` in `true_effects`; C2 grades the DiD against tau_did_truth; the 5f gate compares tau_abm_did (simulator's rollout run through the same estimator) against tau_qe, and tau_abm against tau_true (sign + 3x) | Under interference the two estimands measure different quantities; grading each estimator against the estimand it actually identifies is the honest version of "the DiD recovers the truth" |
| 2026-07-20 | Plain TWFE `post` regression for the staggered DiD | With staggered adoption and a phased-in (dynamic) effect, TWFE makes forbidden already-treated-vs-newly-treated comparisons: at the smoke seed it returns −0.04 for a +0.36 estimand | `causal/did.py` implements the Callaway-Sant'Anna group-time ATT with not-yet-treated controls and a firm-cluster bootstrap CI (the PLAN's named "staggered-adoption-robust estimator"; no dedicated package is installed) | The PLAN itself asks for the robust estimator; TWFE is the textbook failure case here |
| 2026-07-20 | C2 asserts on the shipped 20% panel; §7.7 implies conditioning on z closes the audit backdoor | At the smoke world's 40-firm panel the DiD's bootstrap CI is ±0.19 (the sampled draw sits 2.5 SE below the full-population value), and conditioning on the sealed z does not shrink the audit DML's +2.5-SE bias — the residual is enforcement-timing/dynamic feedback, not the static confounder | C2 (`test_causal_recovers_known_effect.py`, slow) validates the estimators on a purpose-generated full-panel world where they have power; the audit tests assert the raw contrast > 2x truth, DML bias > 2 SE with and without z, and document the dynamic-residual finding; the shipped smoke panel keeps its honest wide CI, exercised by the 5f gate | Parameter-recovery philosophy: validate the estimator on data where it has power; do not shrink the science (the 20% panel) to make a test pass, and do not claim z-conditioning fixes what the data say it does not |
| 2026-07-20 | Stage 5f runs the intervention on "the calibrated Mesa ABM" | Mesa 3.5's model wraps `step()` as a zero-argument clock; the per-firm staggered `treatment_start` schedule lives in the tensorized twin, which passed the 32-seed KS Mesa-agreement gate | tau_abm/tau_abm_did run on the tensorized backend with posterior-mean theta from `posterior.nc`; unsampled firms' onsets are imputed from the observed cohort distribution (registry has no region), and the simulated DiD is estimated on the sampled firms only | The tensorized twin is gate-certified equivalent, natively supports the schedule, and its probability outputs cut Monte-Carlo variance |
| 2026-07-20 | §10 Stage 5f runs the intervention on "the calibrated Mesa ABM"; Phase 5 runs the emulator's training rollouts on it too | Mesa 3.5 wraps `step()` as a zero-argument clock and cannot vary the four levers per quarter, which the domain-randomization schedules (random-uniform, scripted, sinusoid, piecewise-constant) require | Added an optional `lever_schedule=(quarters,4)` argument to `rollout_tensorized`; `None` keeps the exact original static-lever computation (verified bit-identical), a schedule overrides levers per step. The dataset collector and OOD grader use it | The tensorized twin is the 32-seed KS-certified equal of the Mesa model and is the only backend that supports the time-varying policies the emulator must generalize across |
| 2026-07-20 | Ray-parallel dataset collection (`scripts/make_emulator_dataset.py`) | Ray 2.56's worker bootstrap fails on this machine (`raylet` subprocess: `ModuleNotFoundError: No module named 'ray'` under the uv venv), hanging rather than raising | `build_dataset` collects serially below dev scale (smoke's 64 episodes finish in seconds) and only reaches for Ray at >=256 episodes, wrapping `ray.get` in a bounded 1800s timeout with a serial fallback | Keeps the collector correct and fast at smoke/dev without depending on a broken Ray worker launch; the parallel path stays available for the full profile on a working cluster |
| 2026-07-20 | §11 family 5 (planning utility) and family 12 (sensitivity) are part of the Phase 5 eval gate | Both need artifacts Phase 5 does not produce: trained RL/Dreamer policies (Stage 10) and the Morris/Sobol screen (Stage 14) | `scripts/eval_emulator.py` runs the ten families it can and records families 5 and 12 with an explicit `status: pending Phase 6` plus the planned comparison and thresholds, rather than skipping them silently or faking numbers | An evaluation report that hides the metrics it cannot yet compute is worse than one that names them; a crashing family exits the driver nonzero so a broken grader never reads as a pass |
| 2026-07-20 | §11 family 6 checks heavy-tailed cascade sizes; family 11 covers Gaussian-vs-discrete-latent and node-head ablations | Cascade-size extraction needs event-level records the Phase-5 aggregate corpus does not carry, and the extra ablation axes each need a separate training run with >=3 seeds (a dev-profile cost) | Family 6 reports the S-curve fit, size gradient, exit-vs-enforcement correlation, and firm-size tail it can compute and marks cascades deferred; family 11 trains and compares the three named arches (`rssm_gnn`/`rssm_flat`/`gru_baseline`) and lists the remaining axes as deferred to the dev run | Reports every stylized fact and ablation the shipped corpus supports, and names the rest as scoped-out rather than pretending they ran |
| 2026-07-21 | §15 dashboard sliders: "enforcement, targeting, phase-in speed, subsidy, fine scale" | The Gymnasium action space wired since Stage 8 (`training/datamodule.ACTION_LOW`/`ACTION_HIGH`, `rules.PolicyLevers`) has exactly 4 fields; no fifth "fine scale" lever exists anywhere in the emulator, ABM, or RL action contract | `visualization/dashboard.py` renders the 4 levers the action space actually has and states in the sidebar that a fine-scale lever is not part of this action space, rather than adding an unwired fifth slider that could not affect any prediction | A slider that does nothing is more misleading to an operator than a sidebar note explaining why it is absent |
| 2026-07-21 | §15 Fig 8 / dashboard fan: "trajectory fans... over 24 quarters... across the posterior", read from the Stage-11 scenario cube | `ensemble/cube.py` writes one terminal row per (policy, posterior-draw, seed) cell — by design (§10 Stage 11), it never stores the intermediate per-quarter aggregates | `visualization/figures.fig_trajectory_fans` and `dashboard._policy_fan` re-run the trained `EmulatorEnv` forward per policy across several seeds and record every quarter, reusing the same rollout mechanics `ensemble/cube.py` uses per cell instead of reading a per-quarter series that was never persisted | Gives the honest quarter-by-quarter fan the figure needs without inventing or interpolating data the cube does not contain |
| 2026-07-22 | Development happens on one machine (macOS arm64, per the Stage-0 recon above) | Work resumed on a Windows x86_64 box: no Docker, no `make` binary, no `uv`, empty `.venv`, and a default console/file encoding of cp1252 instead of UTF-8 | Installed `uv` via `pip install --user uv` (the official `irm \| iex` PowerShell installer's GitHub download was blocked on this network); ran `uv sync --all-extras` (the `dev`-only `make setup` target is not sufficient — `rl`/`bayes`/`causal`/`opt`/`app` extras must be synced explicitly, same as the original Stage-0 recon did) | `uv` is the project's only supported installer per PLAN.md; matches the "every extra resolved" bar the macOS recon already set |
| 2026-07-22 | `write_text()`/`read_text()` calls default to the platform locale encoding | `reports/FINDINGS.md` and the eval-emulator report embed τ/β/Δ/× directly in prose; Windows' cp1252 default crashes both the write (`scripts/eval_emulator.py`, `evaluation/report.py`) and a test that reads the result back (`tests/test_report_contract.py`, 5 call sites) | Pinned `encoding="utf-8"` explicitly at every read/write site that touches these two reports | Every other `write_text()` in the codebase is a `json.dumps(...)` call (ASCII-safe via `ensure_ascii=True` by default), so this was a complete, contained fix rather than a blanket policy change |
| 2026-07-22 | Log messages use `→`/`─` for readability (e.g. `sensitivity/screen.py`, `sensitivity/policy_search.py`, `pipeline.py`'s run-manifest banner) | Python's default `StreamHandler` on Windows opens stdout/stderr with the cp1252 console codepage; those log lines raised `UnicodeEncodeError` inside `logging.Handler.emit`, caught internally and printed as a `--- Logging error ---` traceback instead of the message — non-fatal (pipeline still exits 0) but noisy on every run | Added `export PYTHONUTF8 := 1` to the Makefile's env-export block (alongside `MPLBACKEND`/`JAX_PLATFORMS`/etc.) | PEP 540 UTF-8 mode is the standard fix for this exact class of Windows console bug; a single env var beats hunting down and ASCII-only-ifying every arrow in log strings, and is a no-op on macOS/Linux where UTF-8 is already the default |
| 2026-07-23 | Appendix D CI: `uv sync --extra dev --frozen`, then `make lint/typecheck/test/smoke` | The dev-only sync installs core+dev, but `make smoke` runs the full pipeline, which needs the bayes/causal/rl/opt/app extras | ci.yml adds a `uv sync --all-extras --frozen` step between `make test` and `make smoke` | Appendix D as written cannot run the pipeline; same fact already recorded for local setup (`make setup` → `--all-extras`) |
| 2026-07-23 | §15 statuses: `{DONE, SKIPPED, DEGRADED, FAILED, BLOCKED}` | §15 also mandates "a re-run skips any stage whose outputs exist and whose config hash is unchanged", which is neither DONE (nothing ran) nor SKIPPED (not config-disabled) | The driver reports that re-run skip as a sixth status, `CACHED` (pipeline.py), carrying the prior run's outputs | Collapsing a cache hit into DONE would fake a run, and into SKIPPED would hide that valid outputs exist; manifest/FINDINGS readers must know the sixth value |
| 2026-07-23 | §5 `--isolated-envs`: "each stage group then runs in its own uv-managed venv as a subprocess" | recon, envs, and marl (and tensorized_abm) are in-process pure-core checks with no script entry point; the driver itself already lives in the core venv | `isolated_envs=true` routes every script-backed stage (STAGE_SCRIPTS in stages.py) through `uv run` in a per-extras-group venv (`.venv-core/-bayes/-causal/-rl/-opt/-app`, synced once per group); the in-process core-only stages keep running in the driver process | Those stages need exactly the core env the driver runs in, so a subprocess venv would add cost while isolating nothing; every extras-bearing stage is isolated |
| 2026-07-23 | §6 lists `configs/hydra/launcher/{basic,joblib,submitit_slurm}.yaml`; Appendix A's `slurm` extra carries only `hydra-submitit-launcher` | The yaml group was absent and no extra carried `hydra-joblib-launcher`, so `hydra/launcher=joblib` could not work anywhere | Added the three launcher yamls (mirroring each plugin's registered defaults) and `hydra-joblib-launcher>=1.2` in the existing `slurm` extra; `uv lock` refreshed | The slurm extra is already the "sweep launchers" home and renaming it is not allowed; a separate extra for one small pure-python plugin is not worth another matrix cell |
| 2026-07-23 | §7.4 peer share `n_{i,t−1} = Σ_l w_il·alive_l·y_{l,t−1} / Σ_l w_il` — all neighbours in the denominator | `rules.neighbour_share` divides by alive neighbours only (`den = A @ alive`), so a dead neighbour drops out of the mean instead of counting as non-compliant; the tensorized ABM and the ingest reconstruction use the same denominator | Documented (comment at the function); code unchanged — β_peer's scale is defined w.r.t. the alive-neighbour share, consistently across DGP, ABM, and analyst regressors | Self-consistency is what parameter recovery needs; flipping the denominator now would silently rescale β_peer's meaning mid-project |
| 2026-07-23 | §7.5 `φ(t) = min(1, t/L)` — zero salience in the onset quarter | `rules.phase_progress` counts the onset quarter as quarters-since-start = 1, so φ = 1/L at onset (0 strictly before); `ingest.py` reconstructs the identical `(quarter − treatment_quarter + 1)/L` | Kept the +1 convention and documented it | The plan's literal form zeroes enforcement in the onset quarter and would kill the DiD event-time-0 effect; the ingest mirror keeps the fitted model well specified |
| 2026-07-23 | §7.4 uses one fine scale `Φ` in both `q_it` and `F_i = min(Φ·R_i, f_cap·R_i)` | The constant is split: `Constants.risk_scale` (4.0) is §7.4's Φ in the perceived-risk equation `q_it`; `Constants.fine_rate` (0.08) is Φ in the actual fine; `Constants.fine_cap` (0.15) is `f_cap` but `min(fine_rate, fine_cap)` = min(0.08, 0.15) can never bind, so the cap is currently dead in the fine formula | Documented the symbol map (risk_scale ↦ Φ in q_it, fine_rate ↦ Φ in F_i, fine_cap ↦ f_cap, unused); values unchanged | One Φ cannot serve both units — a utility-scale risk multiplier and a fraction-of-revenue fine; the cap stays as the named §7.4 symbol should fine_rate ever be raised above it |
| 2026-07-23 | §7.6 reward exit term `− w_x · exit_rate_cum(t)` | `rules.regulator_reward` uses `− w_x · max(0, exit_rate_cum(t) − baseline.exit_rate_cum)` — the cumulative exit-rate delta from the episode baseline | Kept the delta form (tested identical to the plan's form in Regime F, where the fresh-episode baseline has zero exits) | Charging the policy for exits that predate it would penalize every action equally and cancel out of policy comparisons; the delta is the causally attributable term |
| 2026-07-23 | §10 Stage 2 PyG feature list: firm `[size, data_intensity, sector 1-hot, compliant, alive, margin, cost_share]`; segment `[weight, privacy_sensitivity, trust]`; regulator `[budget_used, targeting, phase_progress]`; association `[publicity]` | `to_pyg.node_feature_contract` splits static vs dynamic: firm static = `(size_decile_norm, data_intensity, cost_index_norm, sector 1-hot)` with `(compliant, alive, margin, cost_share)` dynamic (Phase 5); segment static = `(privacy_bucket_norm_imputed, privacy_bucket_observed)` with `(trust,)` dynamic; regulator static = `(bias,)` with `(budget_used, targeting, phase_progress)` dynamic; association static = `(bias,)` with `(publicity,)` dynamic; segment `weight` is dropped from node features entirely (it is not in the observed corpus — it is carried in `SegmentAttributes.weight`/the budget instead) | Static Stage-2 tensors hold observed registry/survey fields only (decile not true size, noisy cost_index, survey-median privacy + observed flag); quarter-varying state attaches in Phase 5 | §10's literal list mixes truth-only and time-varying fields into a static artifact, contradicting §7.2's "observed attributes only"; resolved toward the firewall |
| 2026-07-23 | §8 aggregate series: `hhi_obs ... (each + N(0, sigma_obs))` — additive noise on every column | `observation.aggregate_series` applies `hhi·(1 + N(0, σ_obs))` — multiplicative — to HHI only; the other four aggregates stay additive as written | Kept multiplicative noise for HHI | Additive N(0, 0.01) is invisible on a 0–10,000-scale series; a relative error is the honest measurement model for an index, and σ_obs keeps one meaning across columns |
| 2026-07-23 | §10 Stage 8: a `graph_obs=True` variant returns a `Dict` space carrying the `HeteroData` for the Dreamer agent | The flag existed (`env.graph_obs` in types.py and configs/env/*.yaml) but no code read it — setting it silently changed nothing; the Dreamer agent trains on imagined latent rollouts inside the world model and never consumes graph observations through the env | Removed the dead `env.graph_obs` flag from `EnvCfg` and both env yamls; the Dict/HeteroData env variant is not built | §0 rule 12: a knob that silently changes nothing is worse than no knob, and the one consumer the plan named for it never needed the graph observation path |
| 2026-07-23 | §10 Stage 3 / §16 g14: vectorize the firm decision step with NumPy across the AgentSet if the Python loop is too slow, with `abm.vectorized` declared in the config | The Mesa model is unconditionally vectorized — `RegulationModel._step_impl` computes the whole firm population through the shared NumPy `rules.*` functions and the Mesa agents are thin inspectable views over that array state — so `abm.vectorized` was declared and never read | Removed the dead `abm.vectorized` flag from `AbmCfg` and configs/abm/default.yaml | There is no per-agent scalar code path for the flag to switch back to; it could never change behavior |
| 2026-07-23 | §10 5d E-value "on the CI bound" | The E-value's confidence bound was fabricated as ±10% of the point estimate (`refute.py`), making it decorative | `_estimate_ci_low` now reads DoWhy's real 95% CI (`estimate.get_confidence_intervals`), falling back to a standard-error normal bound, and only then to a wide heuristic | The E-value must reflect the estimate's actual precision, not an invented interval |
| 2026-07-23 | §10 5d "the informative one is add-unobserved-common-cause" | `refute.py`'s docstring named this refuter central but the code never ran it | Added `_unobserved_zero_crossing`: sweeps DoWhy's `add_unobserved_common_cause` at increasing symmetric strength and reports (via `RefutationReport.unobserved_zero_crossing`) the interpolated confounding strength at which the biased estimate crosses zero, else None | PLAN 5d calls this the correct diagnosis of the trap; it was absent |
| 2026-07-23 | §10 5b "Report both" identifiability on analyst and true DAGs | Only the analyst DAG's identification was run (inside `refute_audit`); the true DAG's was never reported | `causal_analysis.py::_identifiability_report` runs `identify_effect` on both DAGs. Finding: because §7.7 wires `z -> compliant_next` only (z does NOT cause audited) and the backdoor runs through size, DoWhy conditions on `size_decile` and declares BOTH identifiable — the real trap is that `size_decile` is a coarsening of the continuous size the confounding flows through, so the "identified" estimand stays biased | Honest to this DGP: the failure here is coarsening, not symbolic non-identification; the four-number gate and E-value quantify the residual bias |
| 2026-07-23 | §10 5f `causal.on_disagreement=recalibrate` (default): fold the DiD in as a Stage-4b moment penalty and re-run 4->5 once | Neither `recalibrate` nor `report` branched — the gate wrote the discrepancy artifact and continued regardless; `calibration.did_penalty` was never read | `validate_simulator.py` now branches on FLAG: `report` is fully wired; `recalibrate` logs a loud warning that automatic re-running is not built in this profile and continues in report mode (never a silent no-op, never a loop). The 5f gate PASSES at smoke, so this path is not exercised in the shipped run | §0 rule 12: the gate passes, so automatic recalibration adds orchestration risk for a path that never triggers; the honest fallback is a loud report, recorded here |
| 2026-07-23 | §10 6+7 imagination decodes symlog aggregates, re-encoded for the next step | The `gru_baseline` imagination branch fed `aggregate_head(core)` (symlog space) straight into the encoder, which consumes NATURAL-units aggregates (teacher forcing feeds it the raw observation aggregates); the `rssm_gnn` branch correctly `symexp`'d first | Added `symexp_clamped` to the gru_baseline branch (`world_model._imagine_step_raw`), matching rssm_gnn | Without it the baseline imagines from HHI~9 instead of HHI~thousands, unfairly handicapping exactly the ablation family 11 uses to rank architectures |
| 2026-07-23 | (audit flag) imagination pairs step-t firm features with step-(t+1) decoded aggregates in the encoder, unlike teacher forcing | Investigated and found NO effect: for `rssm_gnn` the encoder's per-firm `firm_emb` output comes from the GNN over node features only — the aggregate argument feeds solely the pooled `embed`, which the node path discards (`_`). The firm/macro frame pairing (firm_t + macro-context_{t+1} -> node_{t+1}) already matches teacher forcing | No code change; recorded so the analysis is not re-litigated | Changing it would force a full retrain to validate a path whose output provably does not depend on the flagged argument |
| 2026-07-23 | §10 checkpoint `extras` | `train_emulator.py` wrote extras without `n_firms`, so EmulatorEnv warned and fell back to the initial frame, and the sensitivity search silently injected `cfg.population.n_firms` | `train_emulator.py` now writes `n_firms` into extras; `evaluation/sensitivity.py`'s manual injection removed (EmulatorEnv reads extras, with its initial-frame fallback for pre-existing checkpoints) | One authoritative source for the trained firm count instead of three inconsistent backfills |
| 2026-07-23 | §11 family 10 backtest "train the emulator on Regime P q1-12, predict q13-24" | `backtest.evaluate` filters the first 12 quarters of each held-out episode and predicts forward with the already-trained emulator, rather than training a separate emulator on q1-12 only | Kept the filter-and-predict form (no separate training run) | A dedicated q1-12 training run is a dev-profile cost; the held-out-window prediction with no peeking is the honest core of the backtest and is what the coverage threshold grades. Full retrain-on-window deferred to the dev/full profile |
| 2026-07-23 | §11 family 4 DTW "against a random-policy baseline" | `dtw.evaluate` baselines each imagined trajectory against a *different episode's* real trajectory (shuffled pairing), not a random-policy rollout | Kept the shuffled-pairing baseline (reported, not gated) | Shuffled pairing measures episode-specific signal with zero extra rollouts; a dedicated random-policy DTW baseline is a deferred refinement on a non-gated metric |
| 2026-07-23 | §10 6+7 "torch.amp on GPU; emulator.epochs" | `torch.amp` autocast is not wired (CPU-first build; amp is a GPU speedup only), and `emulator.epochs` is carried in configs but the training loop is step-based (`train_steps`), so epochs is inert | Left amp out (no GPU in the required path) and kept `epochs` as a documented-inert knob rather than removing it from every profile | amp buys nothing on CPU (the portability floor); removing `epochs` touches smoke/dev/full and the ablation configs for no behavioral gain |
| 2026-07-23 | §8/§18 ensemble cube dims `(policy, draw, seed, quarter, variable)` in Zarr | `ensemble/cube.py` wrote only a terminal-row Parquet (a prior DEVIATIONS row mislabeled this "by design") — no per-quarter axis, no Zarr | `_rollout_cell` now records the per-quarter outcome vector; `build_cube` returns an xarray `Dataset` dimensioned exactly `(policy, draw, seed, quarter, variable)` (9 variables per §8), NaN-padded past early collapse, written to `artifacts/ensemble/cube.zarr`. Terminal Parquet kept for DuckDB/summary. `p_backfire_by_policy` (§18) now recorded for every policy | §18 requires the Zarr cube and P(backfire\|policy) explicitly; the terminal-only cube did not satisfy it. This supersedes the earlier "terminal-only by design" row |
| 2026-07-23 | §14a emulator-vs-ABM cross-check "correlation of J" | `run_abm_cross_check` correlated emulator **J** (episode return) against ABM **terminal compliance** — different quantities — with no tolerance | Compute the SAME J on the ABM (summed `regulator_reward` over the tensorized rollout, same weights/baseline as `EmulatorEnv`); report the J Spearman correlation AND mean\|ΔJ\|. Reported, not gated | A cross-check that compares two different quantities validates nothing; at smoke the emulator is undertrained so the honest correlation is weak — now visible instead of hidden |
| 2026-07-23 | §10 Stage 11 coverage gate | `enforce_coverage_gate` ran only inside the pipeline driver, so a standalone `make ensemble` at a gating profile never enforced ≥0.85 | `scripts/run_ensemble.py` now calls `enforce_coverage_gate` and exits nonzero on failure (ungated at smoke, as designed) | The gate must hold whether the stage runs via the driver or standalone |
| 2026-07-23 | §14 `scripts/sensitivity.py` summary write | The driver wrote `sensitivity_summary.json` with an Optuna-only schema, clobbering the richer Morris/Sobol/cross-check summary `run_sensitivity` writes to the same path (which `report.py` reads) | The driver now MERGES the Optuna results into the module's summary instead of overwriting it | One canonical `sensitivity_summary.json` carrying both the analysis fields and `optuna_best_J`, which `evaluation/report.py` depends on |
| 2026-07-23 | §3/§10 Stage 11 `draw` = a posterior draw | The emulator marginalizes θ through training-time domain randomization (§10 Stages 6+7), so a cube cell is a distinctly-seeded imagined rollout; `draw` indexes the RNG seed, not a re-drawn θ. Coverage is honest *marginal* coverage; planning utility uses point-θ episodes | Kept the θ-marginal design (documented in `cube.py`'s module docstring) and record it here | Re-drawing θ per cell would double-count the uncertainty the emulator already absorbed; the marginal cube is the correct object for this emulator |
| 2026-07-23 | §10 Stage 10d MARL training wired into the pipeline; §14c BoTorch `bo_policy.py`; §14b Optuna emulator-hyperparameter tuning; `rl.sb3_sac` | `scripts/train_marl.py` (IPPO fallback) exists and runs standalone but is not a pipeline stage; `sensitivity/bo_policy.py`+`scripts/optimize_policy.py` (BoTorch, `--extra opt`, optional per §3) were never built; Optuna was repurposed to a lever search rather than emulator-HP tuning; `cfg.rl.algo` (SAC) is never read (PPO is the required control) | Left as-is and recorded: C6 stays honestly INCONCLUSIVE at smoke (MARL not in the pipeline budget); BoTorch 14c is a named-optional stage (§3 "flip it"); Optuna serves the lever search; SAC config is inert with PPO required | §0 rule 12 / §17 minimal-path: these are optional or standalone tools whose absence is a documented scope choice, not a gate failure. §2 rows 10 and 14 each still have a module+script+test |
| 2026-07-23 | §18/Appendix C Docker image runs `run_pipeline.py profile=smoke`; `make smoke` inside the container | `docker/Dockerfile{,.cuda}` did `uv sync --frozen --no-dev` (core only), so the image could not run its own CMD (needs bayes/causal/rl/opt) and `make smoke` inside it needs pytest (dev) for the slow tests | Both Dockerfiles now `uv sync --frozen --all-extras` (matches `make setup`) | §18 item 2 requires the container to run smoke incl. the slow suite; core-only cannot |
| 2026-07-23 | Appendix C compose MLflow UI `--backend-store-uri file:/work/experiments/mlruns` | The tracker's default backend is `sqlite:///experiments/mlflow.db` (a prior DEVIATIONS row), so a file:mlruns UI shows an empty store | compose now points `mlflow ui` at `sqlite:////work/experiments/mlflow.db` | The UI must read the store the pipeline actually writes |
| 2026-07-23 | §11 eval suite was not a pipeline stage — only `make eval-emulator` ran it | figures 2/5/7/12/13 read `reports/eval/metrics.json`, which the driver never produced, so a clean `make smoke`/`make all` wrote 8/13 figures | Added an `evaluation` driver stage (StagesCfg.evaluation, config.yaml, STAGE_ORDER after sensitivity, HARD_DEP on emulator) running `scripts/eval_emulator.py`. A partial run (some families raise) is DEGRADED not FAILED — metrics.json is still written and figures use what succeeded | §18 items 4 and 10 both require the driver to produce metrics.json and all 13 figures; wiring the existing eval script in is the honest fix |
| 2026-07-23 | §18 C1 verdict / report.py | The C1 verdict checked only max R-hat < 1.01, not the ≥12/16 coverage or divergences the §18 gate names; a figure link was `fig_01_four_numbers.png` vs the written `fig01_...` | C1 SUPPORTED now requires R-hat<1.01 AND divergences==0 AND ≥12/16 coverage (from the parameter_recovery family) and reports the β_peer miss under confounded; figure link corrected. fig 8 uses 50/80/95% bands (was /90) to match §10 and the dashboard | The verdict must reflect the full §18 recovery gate, not convergence alone |
| 2026-07-23 | §15 dashboard backfire indicator | The dashboard built the terminal outcome by copying the baseline aggregate and overriding only compliance and HHI, leaving consumer surplus at its baseline value — so backfire's CS-down leg could never fire | `_policy_fan` now returns the full median terminal aggregate row from the rollout; the dashboard builds the terminal outcome (incl. real consumer surplus) from it and shows the CS delta | A backfire indicator whose third condition can never trigger is a silent liability on a stakeholder dashboard |
| 2026-07-23 | §18 `docker build -f docker/Dockerfile .` succeeds locally | The sandbox's agent proxy denies egress to container registries: both Docker Hub's CDN (`production.cloudfront.docker.com`) and `ghcr.io` blobs (`pkg-containers.githubusercontent.com`) return 403, so no base image (`python:3.11-slim`) or the `ghcr.io/astral-sh/uv` image can be pulled | Recorded the local docker-build DoD item as DEGRADED. The Dockerfiles are corrected (`--all-extras`, above) and syntactically validated; CI (`.github/workflows/ci.yml`/`docker.yml`) builds and runs the image on GitHub-hosted runners where the registries are reachable | The blocker is this environment's network policy, not the Dockerfile or the code; per PLAN/task guidance a registry-blocked local build is DEGRADED-with-reason, not a halt |
| 2026-07-23 | §18 item 4/6/8: `make all` (profile=dev) with dev-scale verdicts (C1 >=12/16 recovery, coverage >=0.85, planning-utility strict) | profile=dev scales the emulator to train_steps=30000 (100x smoke's 300; smoke's emulator alone took 438s -> dev ~12h on this 4-core/15GB box), plus dev calibration/ensemble/sensitivity — a 15-24h single-box run with real OOM/disk risk, not supervisable to completion in one autonomous session | Ran the full pipeline green end-to-end at profile=smoke instead (16/16 stages DONE, 13/13 figures, Zarr cube with the §18 dims, slow suite green); recorded the dev-scale verdicts as DEGRADED/deferred. C1/C3/C5/C6 are honestly INCONCLUSIVE at smoke by design; CI nightly runs profile=dev on a larger runner | §0/task step 8-9: a degraded run that reports itself degraded is acceptable; the pipeline is proven to run and the dev deltas are fidelity, not correctness. The verdict logic (C1 coverage gate, coverage>=0.85 enforcement) is wired and will fire at dev scale |
